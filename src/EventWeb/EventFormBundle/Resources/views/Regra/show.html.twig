{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Regra</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Regra</th>
                <td>{{ entity.regra }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('regra') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('regra_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
